# Changelog

All notable feature work for the `More-Features` branch should be tracked here.

## v1.09 - 2026-04-16

Current feature branch state.

Added:
- visible app version label in the launcher UI
- HTML title updated to include the current version
- library search and filtering controls
- folder filter and favorites-only filter
- sort modes for name, last added, last played, and launch count
- import preview flow for folder structure imports
- merge warnings for ambiguous JS/WASM import cases
- backup export/import tools in Settings
- expanded favorites panel with quick-launch cards
- configurable favorite slot count in Settings
- Playwright browser smoke-test harness

Notes:
- `bare` remains the simpler baseline branch
- `More-Features` is the active feature branch
